The Language GraphL
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of GraphL==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.






UIdent literals are recognized by the regular expression
`````upper (letter | digit | '_')*`````

LIdent literals are recognized by the regular expression
`````lower (letter | digit | '_')*`````

Wild literals are recognized by the regular expression
`````'_' char*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in GraphL are the following:
  | ``Nil`` | ``components`` | ``edges`` | ``empty``
  | ``false`` | ``in`` | ``let`` | ``null``
  | ``rec`` | ``sink`` | ``sinks`` | ``source``
  | ``sources`` | ``true`` | ``vertices`` | 

The symbols used in GraphL are the following:
  | | | ( | , | )
  | = | < | > | ;
  | { | } | <- | +
  | * | - | ?( | <?
  | ?> | [ | ] | {@
  | @} | {- | -} | {*
  | *} | || | & | <??
  | ??> | ??( | ' | ~
  | (( | )) | [[ | ]]

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of GraphL==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //GraphExpr// | -> | //GraphExpr// ``|`` //GraphExpr1// 
  |  |  **|**  | //GraphExpr1// 
  | //GraphExpr1// | -> | //SelectionExpr// 
  |  |  **|**  | //EdgePlus// ``(`` //VertexSelectionExpr// ``,`` //VertexSelectionExpr// ``)`` 
  |  |  **|**  | ``(`` ``rec`` //UIdent// //Formals// ``=`` //GraphExpr2// ``)`` //Actuals// 
  |  |  **|**  | //GraphExpr2// 
  | //GraphExpr2// | -> | ``<`` //Vertex// ``>`` 
  |  |  **|**  | //UIdent// 
  |  |  **|**  | //UIdent// //Actuals// 
  |  |  **|**  | ``Nil`` 
  |  |  **|**  | ``(`` //GraphExpr// ``)`` 
  | //Formals// | -> | ``(`` //[LIdent]// ``;`` //[LIdent]// ``)`` 
  |  |  **|**  | ``(`` //[LIdent]// ``)`` 
  | //Actuals// | -> | ``<`` //[VertexActual]// ``;`` //[EdgeActual]// ``>`` 
  |  |  **|**  | ``<`` //[VertexActual]// ``>`` 
  | //SelectionExpr// | -> | ``let`` //[Binding]// ``in`` //GraphExpr2// 
  |  |  **|**  | //Comprehension// 
  | //VertexSelectionExpr// | -> | ``let`` //[VertexBinding]// ``in`` //GraphExpr2// 
  |  |  **|**  | //VertexComprehension// 
  | //Comprehension// | -> | ``{`` //Generator// ``|`` //[ConditionOrDecl]// ``}`` 
  | //VertexComprehension// | -> | ``{`` //VertexGenerator// ``|`` //[ConditionOrDecl]// ``}`` 
  | //EdgeComprehension// | -> | ``{`` //EdgeGenerator// ``|`` //[ConditionOrDecl]// ``}`` 
  | //GraphComprehension// | -> | ``{`` //GraphGenerator// ``|`` //[ConditionOrDecl]// ``}`` 
  | //VertexBinding// | -> | //VertexDeconstructor// ``=`` //Vertex// 
  | //EdgeBinding// | -> | //EdgeDeconstructor// ``=`` //EdgeExpr// 
  | //GraphBinding// | -> | //GraphDeconstructor// ``=`` //GraphExpr// 
  | //Binding// | -> | //VertexBinding// 
  |  |  **|**  | //EdgeBinding// 
  |  |  **|**  | //GraphBinding// 
  | //VertexGenerator// | -> | //VertexDeconstructor// ``<-`` //VertexCollection// 
  | //EdgeGenerator// | -> | //EdgeDeconstructor// ``<-`` //EdgeCollection// 
  | //GraphGenerator// | -> | //GraphDeconstructor// ``<-`` //GraphCollection// 
  | //Generator// | -> | //VertexGenerator// 
  |  |  **|**  | //EdgeGenerator// 
  |  |  **|**  | //GraphGenerator// 
  | //VertexExpr// | -> | //VertexExpr// ``+`` //VertexExpr1// 
  |  |  **|**  | //VertexExpr1// 
  | //VertexExpr1// | -> | //VertexExpr1// ``*`` //VertexExpr2// 
  |  |  **|**  | //VertexExpr2// 
  | //VertexExpr2// | -> | //Vertex// 
  |  |  **|**  | //LIdent// 
  |  |  **|**  | ``-`` //VertexExpr2// 
  |  |  **|**  | ``(`` //VertexExpr// ``)`` 
  | //VertexDeconstructor// | -> | //VertexExpr// 
  | //VertexActual// | -> | //VertexExpr// 
  | //EdgeExpr// | -> | //Edge// 
  |  |  **|**  | //Edge// ``(`` //VertexExpr// ``,`` //VertexExpr// ``)`` 
  | //EdgePlus// | -> | //Edge// 
  |  |  **|**  | //Wild// 
  | //EdgeLabel// | -> | //Edge// 
  |  |  **|**  | //LIdent// 
  | //EdgeExprPattern// | -> | //EdgeLabel// 
  |  |  **|**  | //EdgeDeconstructor// 
  | //EdgeActual// | -> | //EdgeLabel// 
  | //EdgeDeconstructor// | -> | //EdgeLabel// ``?(`` //VertexDeconstructor// ``,`` //VertexDeconstructor// ``)`` 
  | //GraphDeconstructor// | -> | //GraphDeconstructor// ``*`` //GraphDeconstructor1// 
  |  |  **|**  | //GraphDeconstructor1// 
  | //GraphDeconstructor1// | -> | ``<?`` //VertexDeconstructor// ``?>`` 
  |  |  **|**  | //EdgeLabel// ``?(`` //GraphDeconstructor2// ``,`` //GraphDeconstructor2// ``)`` 
  |  |  **|**  | //GraphDeconstructor2// 
  | //GraphDeconstructor2// | -> | //GraphExpr// 
  |  |  **|**  | ``[`` //GraphDeconstructor// ``]`` 
  | //ConditionOrDecl// | -> | //Condition// 
  |  |  **|**  | //Decl// 
  | //Decl// | -> | //Generator// 
  |  |  **|**  | ``let`` //Binding// 
  | //VertexCollection// | -> | //VertexComprehension// 
  |  |  **|**  | //VertexExtension// 
  | //EdgeCollection// | -> | //EdgeComprehension// 
  |  |  **|**  | //EdgeExtension// 
  | //GraphCollection// | -> | //GraphComprehension// 
  |  |  **|**  | //GraphExtension// 
  | //VertexExtension// | -> | ``{@`` //[Vertex]// ``@}`` 
  |  |  **|**  | //VertexExtensionBuiltin// 
  | //EdgeExtension// | -> | ``{-`` //[Edge]// ``-}`` 
  |  |  **|**  | //EdgeExtensionBuiltin// 
  | //GraphExtension// | -> | ``{*`` //[GraphExpr]// ``*}`` 
  |  |  **|**  | //GraphExtensionBuiltin// 
  | //VertexExtensionBuiltin// | -> | ``vertices`` ``(`` //GraphExpr// ``)`` 
  |  |  **|**  | ``sources`` ``(`` //GraphExpr// ``)`` 
  |  |  **|**  | ``sinks`` ``(`` //GraphExpr// ``)`` 
  | //EdgeExtensionBuiltin// | -> | ``edges`` ``(`` //GraphExpr// ``)`` 
  | //GraphExtensionBuiltin// | -> | ``components`` ``(`` //GraphExpr// ``)`` 
  | //Condition// | -> | //Condition// ``||`` //Condition1// 
  |  |  **|**  | //Condition1// 
  | //Condition1// | -> | //Condition1// ``&`` //Condition2// 
  |  |  **|**  | //Condition2// 
  | //Condition2// | -> | //StructureCondition// 
  |  |  **|**  | //GroundCondition// 
  |  |  **|**  | ``(`` //Condition// ``)`` 
  | //StructureCondition// | -> | //StructureCondition// ``*`` //StructureCondition1// 
  |  |  **|**  | //StructureCondition1// 
  | //StructureCondition1// | -> | ``<??`` //VertexStructureCondition// ``??>`` 
  |  |  **|**  | //EdgeLabelCondition// ``??(`` //StructureCondition2// ``,`` //StructureCondition2// ``)`` 
  |  |  **|**  | //StructureCondition2// 
  | //StructureCondition2// | -> | ``'`` //GraphExpr// 
  |  |  **|**  | ``[`` //StructureCondition// ``]`` 
  | //VertexStructureCondition// | -> | //VertexStructureCondition// ``+`` //VertexStructureCondition1// 
  |  |  **|**  | //VertexStructureCondition1// 
  | //VertexStructureCondition1// | -> | //VertexStructureCondition1// ``*`` //VertexStructureCondition2// 
  |  |  **|**  | //VertexStructureCondition2// 
  | //VertexStructureCondition2// | -> | //Vertex// 
  |  |  **|**  | //AtomicFormula// 
  |  |  **|**  | ``-`` //VertexStructureCondition2// 
  |  |  **|**  | ``(`` //VertexStructureCondition// ``)`` 
  | //EdgeLabelCondition// | -> | //Edge// 
  |  |  **|**  | //AtomicFormula// 
  | //GroundCondition// | -> | //VertexBuiltin// 
  |  |  **|**  | //GraphBuiltin// 
  |  |  **|**  | //AtomicFormula// 
  |  |  **|**  | ``~`` //GroundCondition// 
  | //VertexBuiltin// | -> | ``sink`` ``(`` //VertexExpr// ``)`` 
  |  |  **|**  | ``source`` ``(`` //VertexExpr// ``)`` 
  | //GraphBuiltin// | -> | ``empty`` ``(`` //GraphExpr// ``)`` 
  | //AtomicFormula// | -> | ``true`` 
  |  |  **|**  | ``false`` 
  |  |  **|**  | ``null`` 
  | //[ConditionOrDecl]// | -> | **eps**
  |  |  **|**  | //ConditionOrDecl// 
  |  |  **|**  | //ConditionOrDecl// ``,`` //[ConditionOrDecl]// 
  | //[Condition]// | -> | **eps**
  |  |  **|**  | //Condition// 
  |  |  **|**  | //Condition// ``,`` //[Condition]// 
  | //[Vertex]// | -> | **eps**
  |  |  **|**  | //Vertex// 
  |  |  **|**  | //Vertex// ``,`` //[Vertex]// 
  | //[Edge]// | -> | **eps**
  |  |  **|**  | //Edge// 
  |  |  **|**  | //Edge// ``,`` //[Edge]// 
  | //[VertexActual]// | -> | **eps**
  |  |  **|**  | //VertexActual// 
  |  |  **|**  | //VertexActual// ``,`` //[VertexActual]// 
  | //[EdgeActual]// | -> | **eps**
  |  |  **|**  | //EdgeActual// 
  |  |  **|**  | //EdgeActual// ``,`` //[EdgeActual]// 
  | //[GraphExpr]// | -> | **eps**
  |  |  **|**  | //GraphExpr// 
  |  |  **|**  | //GraphExpr// ``,`` //[GraphExpr]// 
  | //[LIdent]// | -> | **eps**
  |  |  **|**  | //LIdent// 
  |  |  **|**  | //LIdent// ``,`` //[LIdent]// 
  | //[Binding]// | -> | **eps**
  |  |  **|**  | //Binding// 
  |  |  **|**  | //Binding// ``;`` //[Binding]// 
  | //[VertexBinding]// | -> | **eps**
  |  |  **|**  | //VertexBinding// 
  |  |  **|**  | //VertexBinding// ``;`` //[VertexBinding]// 
  | //Edge// | -> | ``((`` //GraphExpr// ``))`` 
  |  |  **|**  | ``((`` //Integer// ``))`` 
  |  |  **|**  | ``((`` //String// ``))`` 
  | //Vertex// | -> | ``[[`` //GraphExpr// ``]]`` 
  |  |  **|**  | ``[[`` //Integer// ``]]`` 
  |  |  **|**  | ``[[`` //String// ``]]`` 


